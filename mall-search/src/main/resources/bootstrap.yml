# 先把配置都放本地

server:
  port: 12000

spring:
  application:
    name: mall-search
  cloud:
    nacos:
      # 配置中心相关
      config:
        server-addr: 127.0.0.1:8848
      # 注册中心相关
      discovery:
        server-addr: 127.0.0.1:8848